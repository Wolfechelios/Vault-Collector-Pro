import React from 'react';
import { parseVisionText } from '@vault/vision';
export function MobileCapture({onCapture}:{onCapture:(value:any)=>void}) { const[text,setText]=React.useState(''); const[result,setResult]=React.useState<any>(null); function analyze(){const parsed=parseVisionText(text);setResult(parsed);onCapture(parsed);} return <section><p className="eyebrow">OFFLINE CAPTURE</p><h2>Analyze an item</h2><label className="capture">＋ Capture or add photos<input hidden type="file" accept="image/*" capture="environment" multiple/></label><textarea value={text} onChange={e=>setText(e.target.value)} placeholder="Visible label text, model, serial, ISBN, condition…"/><button onClick={analyze}>Analyze locally</button>{result&&<div className="cards">{result.fields.map((field:any)=><article key={field.field}><small>{field.field} · {Math.round(field.confidence*100)}%</small><h3>{field.value}</h3></article>)}</div>}</section>; }
