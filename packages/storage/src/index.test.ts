import{describe,it,expect}from'vitest';import{normalizeStoragePath,buildStorageTree,descendants}from'./index';describe('storage',()=>{it('normalizes paths',()=>expect(normalizeStoragePath('House > Garage / Bin 1')).toBe('House / Garage / Bin 1'));it('builds hierarchy without duplicates',()=>{const n=buildStorageTree(['House/Garage/Bin 1','House/Garage/Bin 2']);expect(n).toHaveLength(4);expect(descendants(n,'house')).toHaveLength(3)})});
