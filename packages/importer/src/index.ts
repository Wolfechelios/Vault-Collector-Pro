export type ImportRow=Record<string,string>;
export function parseCsv(text:string):ImportRow[]{const rows:string[][]=[];let row:string[]=[];let field='';let quoted=false;for(let i=0;i<text.length;i++){const c=text[i];if(c==='"'){if(quoted&&text[i+1]==='"'){field+='"';i++}else quoted=!quoted}else if(c===','&&!quoted){row.push(field);field=''}else if((c==='\n'||c==='\r')&&!quoted){if(c==='\r'&&text[i+1]==='\n')i++;row.push(field);field='';if(row.some(v=>v.length))rows.push(row);row=[]}else field+=c}row.push(field);if(row.some(v=>v.length))rows.push(row);const headers=(rows.shift()??[]).map(h=>h.trim());return rows.map(values=>Object.fromEntries(headers.map((h,i)=>[h,(values[i]??'').trim()]))) }
export function mapImportRow(row:ImportRow){return{title:row.title||row.name||row['Item Title']||'Imported item',category:row.category||'other',condition:row.condition||'Unknown',quantity:Number(row.quantity||1),brand:row.brand||null,model:row.model||null,year:row.year?Number(row.year):null,sku:row.sku||null,serialNumber:row.serialNumber||row.serial||null,description:row.description||null,notes:row.notes||null}}
