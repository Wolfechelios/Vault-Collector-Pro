import{describe,expect,it}from'vitest';import{buildBreadcrumb,calculateCapacity,createNode,decodeStorageQr,encodeStorageQr,moveNode,planBulkAssignmentMove}from'./index';
describe('storage management',()=>{const root=createNode({id:'root',name:'Home',kind:'property'});const room=createNode({id:'room',name:'Garage',kind:'room',parentId:'root'});const bin=createNode({id:'bin',name:'Bin 1',kind:'bin',parentId:'room',capacityLimit:2});const nodes=[root,room,bin];it('builds breadcrumbs and prevents cycles',()=>{expect(buildBreadcrumb(nodes,'bin').map(n=>n.name)).toEqual(['Home','Garage','Bin 1']);expect(()=>moveNode(nodes,'root','bin')).toThrow(/cycle/)});it('round trips QR payloads and rejects tampering',()=>{const qr=encodeStorageQr('install-1','bin');expect(decodeStorageQr(qr,'install-1').nodeId).toBe('bin');expect(()=>decodeStorageQr(qr+'x')).toThrow(/checksum/)});it('plans atomic bulk moves and capacity warnings',()=>{const plan=planBulkAssignmentMove(['a','b'],'bin',nodes,[]);expect(plan.assignments).toHaveLength(2);expect(plan.moves).toHaveLength(2);expect(calculateCapacity(bin,plan.assignments,nodes).percent).toBe(100);expect(plan.warnings).toHaveLength(1)});});